#include <bits/stdc++.h>
using namespace std;

int main() {
    double a, b, c, d, e, f, g, h, i, j;
    double ma, mg, mh, eh, eg, em;
    cin >> a >> b >> c >> d >> e >> f >> g >> h >> i >> j;

    ma = ((a+b+c+d+e+f+g+h+i+j) / 10);
    cout << "Média aritmética é" << ma << endl;

    mg = (a*b*c*d*e*f*g*h*i*j);

    

















    return 0;
}